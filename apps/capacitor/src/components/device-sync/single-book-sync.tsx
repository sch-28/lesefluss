import type { DeviceCapabilities } from "../../services/devices/capabilities";

export type SingleBookSyncProps = {
	caps: DeviceCapabilities;
};

/** Placeholder. Will host the single-book sync UI currently at routes/tabs/settings/device.tsx. */
export function SingleBookSync(_props: SingleBookSyncProps) {
	return null;
}
